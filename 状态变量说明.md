# DICK 状态变量与标签说明（普通用户版）

> 这份文档告诉你怎么写"状态栏 / 战斗栏 / 好感度 / 机制卡"里的变量和标签。
> 你不用懂编程——照着格式抄就行。AI 会在回复里输出标签（如 `[aff:+3]`），DICK 自动解析并更新状态栏。

---

## 一、一句话原理

DICK 的"状态"由两部分组成：

1. **机制卡配置**（你在角色编辑里填的）——定义有哪些状态字段、好感上限等
2. **AI 回复里的标签**（如 `[aff:+3]`、`[体力:-10]`）——每轮聊天自动更新状态

AI 回复末尾带标签 → DICK 解析 → 更新状态栏 → 标签从显示中隐藏（玩家看到的是干净的文字）。

---

## 二、标签总表（AI 输出、也可手动写在消息里测试）

标签格式统一：`[键:值]`（半角方括号 + 英文冒号，AI 已按此训练）

| 标签 | 作用 | 示例 | 说明 |
|---|---|---|---|
| `[aff:+N]` | 好感度变化 | `[aff:+5]` | **百分比**：+5 = 上限的 5%（上限 100 时 = +5 点） |
| `[aff:-N]` | 好感度降低 | `[aff:-3]` | 同上，减 |
| `[ph:-N]` | 玩家 HP 减少 | `[ph:-10]` | 玩家被打掉 10 血（需玩家卡配了战斗属性） |
| `[ph:+N]` | 玩家 HP 恢复 | `[ph:+15]` | 玩家回血 |
| `[状态键:值]` | 整数状态（绝对值） | `[体力:80]` | 体力直接设为 80 |
| `[状态键:+N]` | 整数状态（相对增减） | `[体力:-10]` | 体力在当前值上减 10 |
| `[状态键:枚举值]` | 枚举状态（直接赋值） | `[心情:开心]` | 心情设为"开心" |

**注意**：
- 好感度是**百分比制**：`[aff:+5]` = 上限的 5%。上限 100 时和"5 点"一样；上限 1314 时 = 65.7 → 66 点。
- 整数状态支持 `+N`/`-N` 相对增减和 `N` 绝对赋值；枚举状态只能直接赋值（如 `[心情:开心]`）。
- 未知键的标签**原样保留**（不会误删正文里的 `[xx:yy]`）。

---

## 三、状态栏显示什么

状态栏（机制卡三栏里的"状态栏"）显示的是**机制卡配置里定义的字段**。你在角色编辑 → 机制卡里填：

```
心情|enum|平静|平静,开心,害羞,生气,委屈
体力|int|100|0-100
```

格式：`键|名称|类型|初始值|范围或选项`

- `int` 类型：范围写 `0-100`，显示为进度条 + 数值
- `enum` 类型：选项写 `平静,开心,...`，显示为文字
- 类型 `int` 且上限 > 1000 的（如 atk/def）：只显示数值，不画进度条

AI 每轮回复后用 `[心情:开心]` / `[体力:-10]` 更新它们，状态栏实时刷新。

---

## 四、战斗栏变量（战斗系统）

玩家卡/角色卡配了战斗属性后，战斗栏显示这些变量：

### 4.1 角色侧（敌方）属性

机制卡战斗里填的基础属性，如：

```
hp|生命|100|100
atk|攻击|10
def|防御|5
```

格式：`键|名称|初始值|上限(可空)`

### 4.2 玩家侧属性（玩家卡同规格）

玩家角色卡里填的战斗属性，默认：`hp=100, atk=10, def=5`。
公式变量名固定：**`player_atk` / `player_def` / `player_hp`**。

### 4.3 招式公式可用的变量

伤害公式里可用：
- 角色属性键（hp/atk/def/spd/mp... 直接用键名）
- 玩家属性：`player_atk`、`player_def`、`player_hp`

示例公式：
```
damage=max(1, player_atk*2-def)     # 玩家攻击 ×2 - 敌方防御，至少 1
crit_chance=0.1                     # 暴击率 10%
crit_mult=2                         # 暴击倍率 2 倍
```

### 4.4 玩家 HP 标签

战斗中 AI 攻击玩家 → 输出 `[ph:-N]` 扣玩家血；玩家回血 → `[ph:+N]`。

---

## 五、好感度百分比速查（重要）

好感度**全部按百分比**算（METTERTOOLS 也按百分比）：

| 你想表达 | 标签 |
|---|---|
| 好感 +5%（上限100时=+5） | `[aff:+5]` |
| 好感拉满到上限 | `[aff:+100]` 或 `/mettertools 100` |
| 好感降到 0 | `[aff:-100]` |
| 好感小幅波动 | `[aff:+2]` ~ `[aff:+5]` |

> `/mettertools 90` = 一键把好感填到上限的 90%（罪恶都市梗）。

---

## 六、ROLL 概率表（点选项时触发）

点 GAL 选项时按概率隐藏 ROLL（概率不公布）：

| 档位 | 默认概率 | 效果 |
|---|---|---|
| 坍缩 | 十万分之一（0.001%） | 战斗数值全部坍缩为 2000 |
| 天选 | 千分之一（0.1%） | 奇迹事件 |
| 暴击 | 10% | 好感效果 ×2 |
| 稀有 | 4% | 稀有事件 |
| 大失败 | 2% | 好感反转（-aff） |

---

## 七、常见问题

**Q：状态栏不显示？**
机制卡里没配置字段，或没勾选"启用"。角色编辑 → 机制卡 → 勾选好感/状态 → 填字段。

**Q：标签没生效？**
检查格式：半角 `[` `]` 和 `:`；键名要和机制卡里填的一致；好感度要先启用。

**Q：`[ph:-10]` 没扣玩家血？**
玩家卡要先配战斗属性（玩家角色卡 → 战斗属性 → 勾选启用），否则玩家没有 hp 可扣。

**Q：战斗栏玩家数值不对？**
玩家属性来自玩家卡的 advanced.battle，没配就用默认 100/10/5。

---

*文档版本 v1.0 —— 对应 DICK 机制卡/战斗/好感度当前实现。*
