# Direct-Interface Cork-bore Kit（DICK）便携版使用说明书

> 免安装 · 解压即用 · 数据随身带
> 适用版本：打包版 DICK.exe（v2.0）—— 无需安装 Python，无需任何依赖

---

## 一、这是什么

DICK 是一个桌面 AI 助手，把角色扮演、跑团、办公写作、财报分析装进一个绿色文件夹：

- 角色扮演：角色卡、群聊、单推模式、多候选回复
- 跑团工具：玩家角色卡、世界卡、平行世界、世界书、记忆链
- 办公写作：公文模式、Word/Excel 读写与规范排版
- 财报分析：政策深度爬取、联网搜索、经济学知识库
- 日文语音：可爱 TTS 声线朗读

便携版特点：所有数据（角色、存档、设置、插件）都保存在 DICK.exe 旁边，整个文件夹拷到 U 盘换台电脑就能接着聊。

### 1.1 友军：你应该试试酒馆（SillyTavern）

DICK 不是孤军。**SillyTavern（酒馆）是我们的友军，同一个沙盒的另一种玩法**：

- **DICK**：轻量、开箱即用、傻瓜友好，一键把角色卡打包成独立 GALGAME（EXE/HTML）
- **酒馆**：硬核、插件生态深不见底、折腾友好，深度玩家乐园

两者共享角色卡生态——**酒馆社区创作的海量角色卡，DICK 直接导入就能用**。我们不做彼此的替代品，而是同一个品类的两极：它造卡，我们成作，合起来才能对抗"云端租客经济"（把数据锁在服务器里收会员费的那种产品）。

> 用 DICK 觉得不够折腾？**去试试酒馆**，那里有整个插件宇宙。
> 用酒馆觉得太重？**回来用 DICK**，九分钟上手，还能把你的作品打包带走。

---

## 二、安装与启动

### 2.1 解压

把下载的压缩包解压，得到一个 DICK 文件夹，里面有：

    DICK.exe          主程序（双击运行）
    _internal         程序引擎与内置资源（只读，别动）
    config.json       全局设置与 API Key
    debug.log         运行日志（排查问题用）

### 2.2 启动

双击 DICK.exe。首次启动会弹出「欢迎」窗口：

1. 点击「购买API」直达 DeepSeek 官网获取 API Key
2. 粘贴 Key → 点「启动聊天」
3. 左侧选角色 → 开始对话

### 2.3 放置位置

- 放哪里都行：桌面、D 盘、U 盘
- 必须整文件夹移动：DICK.exe 与 _internal 必须同目录，不要只拷一个 exe
- 建议避开 C:\Program Files 这类需要管理员权限的目录（写数据会受限）

### 2.4 杀毒软件提示

程序用 PyInstaller 打包，个别杀软可能误报。将 DICK 文件夹加入信任/白名单即可，属正常现象。

### 2.5 首次运行自动释放

首次启动会自动在 exe 旁创建数据文件夹，并释放内置默认内容（只补缺失，绝不覆盖你的数据）：

- 6 个提示词预设（跑团主持人/小说叙事/单推模式/角色单推模式/公文模式/财报模式）
- 4 张示例世界卡（含财报模式经济学规则卡）
- 示例角色（咲、美雪、记者、猫娘萝莉等）与示例玩家角色卡
- 19 个内置插件（含财报助手 v2.0：政策深爬/通用爬取/政策库）；HTML 版设置面板内置「🔌 插件管理」：逐插件启用/禁用 + 声明式参数调校

误删了某个默认内容想恢复？把对应的数据文件夹整个删掉，重启程序会自动重新释放。

---

## 三、快速开始（5 步）

1. 打开「⚙️ 设置」→ 从 **模型商下拉框** 选择一家（内置 14 家、100+ 模型：DeepSeek / OVH 免费链（免 Key）/ 阿里云百炼 / 智谱 / 硅基流动 / Kimi / 火山方舟（豆包）/ 百度千帆 / MiniMax / 阶跃星辰 / OpenAI / Claude / Gemini / Ollama 本地）
2. 点 **「🔑 去官网注册 / 充值」** 直接跳转该厂商官网拿 API Key（免 Key 厂商跳过此步）；选好 **模型**（下拉即可选，也可手输自定义）
3. 填入 API Key 保存——**每个厂商的 Key 会分别记住**，切换厂商不用重填
4. 左侧勾选角色卡（Ctrl/Shift 多选 = 群聊），点「✅ 启动聊天」
5. 输入消息发送（Enter 发送；群聊时 @角色名 内容 指定发言对象）

---

## 四、数据与备份（便携版核心）

所有数据都在 DICK.exe 旁边，不写注册表、不藏系统目录：

| 文件夹/文件 | 内容 |
|---|---|
| saves | 角色卡与聊天存档 |
| worlds | 世界卡与平行世界设定 |
| personas | 玩家角色卡 |
| prompt_presets | 提示词预设 |
| memory | 记忆链存档 |
| plugin_settings | 各插件设置 |
| exports | 办公模式导出的 Word/Excel |
| plugins | 你自己安装的插件（可写） |
| config.json | 全局设置与 API Key |
| debug.log | 运行日志 |

- 备份：把整个 DICK 文件夹复制一份即可
- 换电脑：整个文件夹拷过去
- 更新：用新版覆盖 DICK.exe 与 _internal（数据文件夹保留）；或把 saves/memory 等数据文件夹拷进新版文件夹

---

## 五、角色与群聊

### 5.1 角色卡

- 左侧「📂 存档列表」选择角色（Ctrl/Shift 多选）
- 「➕ 新建角色」「🗑️ 删除」「📤 导出」「📥 导入」

角色卡采用**结构化精细表单**：名字之外还可填写——完整设定（旧版原文，可留空）、外貌、性格、过去经历、说话方式（语气/口癖/句式）、开场白、对话示例、备注。各分节自动拼装进系统提示（【外貌】【性格】【过去经历】……）；编辑旧卡时原文完整保留，不会丢失。✨ AI 起草同样按这套结构产出。
- 酒馆卡导入：「🔄 酒馆卡工具」→「📥 导入酒馆卡」（PNG/JSON，自动分离角色设定与世界书）

HTML 桌面版内置酒馆卡兼容（无需插件）：

- 每张卡右侧 **⬇️ 导出 v2 JSON**、**🖼️ 导出 PNG 嵌卡**（导出的 PNG 可直接扔进酒馆/CharHub 使用）
- **「📥 导入角色卡」**：支持 .json（v1/v2/v3 规范）与 .png/.webp 嵌卡（读取 chara/ccv3 块）；PNG 导入自动把图设为头像，同名卡自动改名避免覆盖
- 导入的卡无损保留原始字段，再导出时按 v2 规范回写；没有头像的卡导出时自动生成渐变首字占位图

### 5.2 群聊

- 多选角色即进入群聊，AI 一次扮演一个角色
- @角色名 内容：指定发言对象；不指定则最相关角色回应
- 「🤖 自动接话」：其他角色自动跟进（默认每轮最多 2 次，可关）
- 回复自动归属发言者，聊天区按角色分色显示

### 5.3 多候选回复（Swipe）

| 命令 | 作用 |
|---|---|
| /swipe | 无候选则生成 3 个，有候选则切换下一个 |
| /swipe back | 上一个候选 |
| /swipe 2 | 跳到第 2 个候选 |

候选数量在「插件管理 → 多候选回复 → ⚙️ 设置」调整（1~5）。

### 5.4 其他对话操作

- 「🔄 重试」重新生成；「✏️ 编辑上一条」修改后重发
- 回复为流式输出（逐字显示）
- HTML 版：气泡悬停出现控制条——✏️ 编辑（用户消息改后开新分支并自动重新生成；AI 消息原地修改）、↻ 重新生成新候选；AI 回复有多个候选时 ◀ n/m ▶ 滑条切换
- 顶栏 **🌿 分支**：列出所有历史分支（含预览），点击即切换；切走的分支不丢失，随时切回

---

## 六、玩家角色卡（跑团 PC）

左侧「🧑 玩家角色」区域：

- 「➕ 新建」：填写姓名/外貌/背景/性格/说话风格/备注（携带物品、目标）
- 「✅ 启用」后：AI 认识你、以你的角色名称呼你；你的消息显示为 [角色名]
- 配置自动保存，重启自动恢复

---

## 七、世界与平行世界

### 7.1 世界卡（多选 = 平行世界，角色可自由穿梭）

- 左侧「🌍 世界卡」多选 = 角色的「可穿梭世界池」；当前所在世界以 ★ 标注
- **🚀 穿越按钮**或 **/穿越 世界名**：把角色移动到指定世界——角色的人设、记忆、对话树完全保留，只有世界设定与世界观条目随行切换
- 角色卡与世界卡**完全解耦**：角色文件不含世界字段、世界文件不含角色字段；删世界不伤角色，删角色不伤世界；同一角色可在一段对话里反复穿梭多个世界
- 世界书条目按「当前世界」生效：人在甲世界时，乙世界的条目不会误触发
- 便携版自带 4 张示例世界卡：神圣罗马帝国、1848 天国人间、僚机计划、财报经济学规则

| 命令 | 作用 |
|---|---|
| /穿越 | 列出全部可选世界（★ 标注当前） |
| /穿越 世界名 | 穿越到指定世界（支持模糊匹配） |

### 7.1b 世界参数（物理系统等）

世界卡编辑对话框内置 **8 项世界参数**：科技水平、超自然体系、物理法则、时间流速、气候环境、地理格局、政治格局、经济体系。保存后自动渲染进世界背景（【世界参数】段落），AI 始终遵守这些设定。✨ AI 起草世界卡时同样会产出参数。

### 7.2 世界书高级逻辑

条目支持字段（在「📖 世界书」编辑器可视化维护）：

| 字段 | 说明 |
|---|---|
| keywords | 关键词（命中即注入） |
| match | any（任一命中）/ all（全部命中）/ regex（正则） |
| weight | 权重，越大越先注入 |
| probability | 概率 0-100（100=必中） |
| depth | 递归链最大长度：1=仅扫用户输入；2=命中内容可再触发一层（上限 4） |
| enabled | 停用开关（关掉的条目不参与匹配） |
| constant | 常驻条目：无需关键词，每轮必注入（不受注入条数上限约束） |
| content | 注入给 AI 的知识 |
| comment | 备注 |

HTML 版在「✏️ 编辑世界」对话框内直接可视化维护条目：启用/常驻复选框、关键词、match 下拉、权重/概率/深度、内容，状态圆点（蓝=常驻、绿=最近消息命中、灰=停用）。

---

## 八、记忆与上下文

### 8.1 记忆链插件（存档扩容）

存档超过 1.5MB 自动归档为 memory/角色.part1.json 链，当前存档只保留最近节点；旧历史永久不丢，并自动生成压缩记忆注入新对话。

| 命令 | 作用 |
|---|---|
| /memory | 记忆链状态 |
| /memory recall [N] | 回溯最近 N 条历史（下次发送附带） |
| /memory summary | 查看压缩记忆 |
| /memory clear | 清除待回溯内容 |

### 8.2 上下文预算

预设行「🧮 预算」下拉（4K~128K）：超预算自动裁剪早期消息；配合「📜 滚动摘要」开关，裁剪前自动把旧历史压缩成摘要注入，记忆不断片。

---

## 九、提示词预设

预设行下拉选择，可随时「✏️ 管理」新建/编辑/删除。便携版内置 7 项：

| 预设 | 说明 |
|---|---|
| 默认 | 角色卡原样 |
| 跑团主持人 | GM 视角：掷骰判定、节奏控制 |
| 小说叙事 | 描写风格、字数控制 |
| 单推模式 | 你是角色的单推人（你推 TA：应援、告白） |
| 角色单推模式 | 角色单推你（专一、吃醋） |
| 公文模式 | 公文助手（自动禁用角色卡） |
| 财报模式 | 宏观分析师（自动载入锁定经济学世界卡） |

---

## 十、办公模式

### 10.1 公文模式

预设选「公文模式」后：角色卡自动清空并锁定；无需选角色即可启动聊天。输入"写一份关于五一放假的通知"即可得到标准公文（标题/正文/落款）。

### 10.2 文档读写

| 操作 | 入口 |
|---|---|
| 读入 Word/Excel 供 AI 分析 | /read、/read <路径>、➕ 面板「📥 读取文档」 |
| 查看/清除文档 | /readinfo、/readclear |
| 生成 Word（7+ 文种） | /doc、➕ 面板「📄 写 Word 文档」 |
| 生成 Excel（9+ 模板） | /office、➕ 面板「📊 写 Excel 表格」 |
| 一键导出最近回复 | ➕ 面板「导出最近回复·Word/Excel」 |

生成文件自动按规范排版：公文 GB/T 9704（页边距/小标宋标题/仿宋正文/首行缩进/行距）、Excel 自动列宽+表头样式+边框。文件保存于 exe 旁的 exports 文件夹。

---

## 十点五、创意工坊

左侧「🧰 创意工坊」按钮（原 Tk 版创意工坊已完整移植到 HTML 版与 Android 版，双端协议一致）：

- **📂 本地页**：角色卡/世界卡双列表 + 预览；📤 导出文件、📂 打开目录、🗑️ 删除
- **🌐 在线页**：连接工坊服务器（地址 + 可选 Key，保存到 workshop_config.json）；🔌 测试连接（显示服务器统计：认证模式/下载/点赞）；🔍 搜索作品；⬇️ 下载到本地（重名自动加序号并刷新列表）；❤️ 点赞；📤 上传本地卡；🗑️ 删除自己的作品

---

## 十一、财报模式

### 11.1 进入

预设选「财报模式」：自动载入并锁定「财报模式_经济学规则」世界卡（货币政策/财政政策/美林时钟/估值/风控/历史危机/政策措辞解码 9 条规则）。

### 11.2 数据获取

| 命令 | 作用 |
|---|---|
| /财报 爬取 | 全源深爬：10 个官方政策源 + 翻页 + 正文 + 关联文件递归 |
| /财报 标题 | 全源标题速览（不抓正文，最快） |
| /财报 爬取 <网址> | 抓取指定红头文件全文 |
| /爬取 <网址> | 通用网页爬取：任意网站正文（新闻/文章/公告） |
| /财报 入库 | 全源深爬存入本地政策库（增量去重） |
| /财报 检索 <关键词> | 从政策库检索相关条目 |
| /财报 定时 开 [小时] | 政策库定时刷新（默认 6 小时）；关 用 /财报 定时 关 |
| /财报 股票 <代码> | A股个股炒股要素分析（见下方说明）；快捷方式 /股票 <代码> |
| /搜索 <关键词> | 联网搜索（DuckDuckGo/Bing，免 Key） |
| /深搜 <关键词> | 搜索 + 自动跟进抓取每条结果的网页正文 |

财报模式下发送消息会自动引用政策库中与话题最相关的条目（policy_db.json 保存在程序旁）。

### 11.2b 内置金融史年表（1617-2026）

政策库内置 **49 条金融史年表**（随程序打包，financial_history.json），覆盖从 1617 年荷兰金融革命到 2026 年 AI 投资浪潮的完整脉络：

- **全球线**：郁金香狂热（1637）、南海泡沫（1720）、大萧条（1929）、布雷顿森林（1944）、尼克松冲击（1971）、沃尔克加息（1979）、广场协议（1985）、日本泡沫（1990）、亚洲金融危机（1997）、次贷危机（2008）、欧债危机（2010）、硅谷银行（2023）……
- **中国线**：改革开放（1978）、沪深交易所（1990）、汇率并轨（1994）、加入 WTO（2001）、股权分置改革（2005）、四万亿（2008）、供给侧改革（2016）、科创板注册制（2019）、三道红线（2021）……
- 每条均含六节：**【事件】【原因】【当时经济条件】【当时科技条件】【当时政治条件】【影响与启示】**，原因部分多因素展开、传导机制详述
- 年表与爬取的政策文章共存于同一政策库：/财报 检索 郁金香 即可查；财报模式下发消息自动引用并标注 finhist:// 出处；重新爬取入库不会覆盖年表（增量去重）
- 财报模式预设规则已要求：分析当前市场时对照历史相似事件，说明历史规律的适用条件与差异

### 11.3 个股炒股要素分析（免 Key 免费数据源）

命令：/财报 股票 <代码> 或 /股票 <代码>（如 /股票 600519、/股票 sh300750，自动识别沪深北交易所）。数据来自腾讯行情（实时）与腾讯前复权日K（160 根），计算并注入上下文：

- 实时行情：现价/涨跌幅/开高低/成交量/成交额/换手率/量比/振幅
- 估值：市盈(动)/(静)/市净率/总市值/流通市值
- 均线 MA5/10/20/60（多头/空头排列判定）
- MACD（DIF/DEA/柱，金叉死叉识别）
- KDJ（K/D/J，超买超卖）
- RSI6/12/24（超买 70 / 超卖 30）
- BOLL 布林带（上下轨位置）
- WR14 威廉指标、ATR14 真实波幅、20 日年化波动率
- 支撑/压力位（20/60 日及区间高低点）
- 量能（5日 vs 10日均量，放量/缩量）
- 信号汇总：逐项偏多/中性/偏空统计 + 一句话结论

结果自动注入文档上下文，随后可直接提问（如：结合技术面分析 600519 后市）。

### 11.4 联动综合分析

命令：/财报 联动 <代码1[,代码2...]> [关键词]（快捷方式 /联动 同上），如：

    /联动 600519,300750 消费 新能源

一键生成多股联动报告：每只个股的完整技术面（上节全部要素）+ 政策库匹配条目（按股票名与关键词检索 policy_db）+ 联动结论（逐股信号汇总、政策面命中数、综合多空判断），整体注入上下文，随后直接让 AI 深度解读联动机会。政策库为空时先运行 /财报 入库。

### 11.5 全市场列表与筛选

命令：/股票列表 [N]（涨幅榜，默认 20）；/股票列表 跌 [N]（跌幅榜）；/全市场 <条件...>（全市场筛选）。

- 数据源：东方财富全市场接口（约 5900 只沪深北 A 股）优先，被限流时自动回退新浪行情中心（此时量比/主力净流入字段不可用，报告会提示）
- 筛选条件：涨幅>5、换手>10、量比>2、市盈<50、市值<500（亿）、流入>1000（主力净流入，万），可任意组合
- 示例：/全市场 涨幅>5 换手>5 市盈<80 市值<500 → 返回命中前 50 只（现价/涨幅/换手/量比/市盈/市值/主力净流入）
- 筛选结果注入上下文，可直接让 AI 进一步分析或配合 /联动 深挖个股

数据注入文档上下文后，直接提问："结合最新政策，分析新能源板块未来走势"。

### 11.3 可靠性设计

- 政策措辞解码规则：自动还原"回升向好=预期引导""不搞大水漫灌=总量克制""防范化解风险=局部出清"等粉饰表述的真实含义
- 分析强制免责声明：仅供参考，不构成投资建议

---

## 十二、日文与语音

### 12.1 日文补丁

| 功能 | 用法 |
|---|---|
| 自动日译输入 | ⚙️ 设置开启（已含假名自动跳过） |
| 中→日 / 日→中 | /jp 文本、/zh 文本 |

### 12.2 可爱日文 TTS

主引擎 VOICEVOX（免费离线动漫声线：ずんだもん、四国めたん、春日部つむぎ等），需另外下载并启动（一次性）：

1. 到 voicevox.hiroshiba.jp 下载 VOICEVOX 并启动
2. 保持它运行，DICK 自动连接本地引擎
3. 回应用点「🔊 朗读」（位于 ➕ 面板）

| 命令 | 作用 |
|---|---|
| /tts | 朗读上一条回复 |
| /tts speakers | 查询全部声优 |
| /tts voice ずんだもん | 切换声优 |
| /tts on / off / stop | 自动朗读开关/停止 |

可爱参数默认已调（音调+0.15/语速1.1/语调1.2），⚙️ 可再调。没装 VOICEVOX 时会自动降级到内置 edge-tts 备胎（需联网）。

---

## 十三、插件体系

### 13.1 管理

HTML 版：设置面板 →「🔌 插件管理」——逐插件启用/禁用，插件声明的参数（数值/开关/下拉/文本）直接在面板里调。

Tk 版：「🛠️ 工具 → 🔧 插件管理」：安装（.py 文件）/ 卸载（保留 .off）/ 启用切换 / ⚙️ 设置（声明式自动生成）/ 重载。

### 13.2 安装新插件

两种方式：

- 插件管理窗口点「➕ 安装」，选 .py 文件
- 直接把 .py 放进 exe 旁的 plugins 文件夹

### 13.3 开发插件（零 Tk 代码）

    from plugin_base import PluginBase

    class MyPlugin(PluginBase):
        name = "我的插件"
        description = "示例"
        enabled = True

        # 声明式设置 → 自动生成设置窗口
        settings_schema = [
            {"key": "max_len", "label": "最大长度", "type": "int",
             "default": 100, "min": 1, "max": 500},
        ]
        # 声明式 UI 按钮 → 自动收入 ➕ 面板
        ui_buttons = [
            {"type": "insert", "label": "🎲 d20", "text": "/r 1d20"},
            {"type": "method", "label": "🖼️ 图片", "method": "show_window"},
        ]

        def on_command(self, command, args): ...
        def on_message_send(self, user_input): ...
        def on_message_received(self, user_input, ai_reply): ...

内置插件清单：骰子大师、记忆链扩容、多候选回复、现代界面、日文补丁、日文朗读(TTS)、世界书编辑器、联网搜索、财报助手、图片导入、酒馆卡片导入、公文模式、通用办公、Word导出、程序员工具、数学运算、Echo、Calculator 等 19 个。

> 📐 **插件编写标准**：完整规范见随包附带的 **`PLUGIN_DEV.md`**（插件文件规范 / 类属性 / 生命周期 / 消息钩子 / 命令钩子 / 声明式设置 / UI 按钮 / 访问 core / 调试发布 / 速查表）。DICK 插件是 **Python 后端插件**——与酒馆的前端 JS extension 不同：它挂接引擎能力（模型/文件/网络/机制卡/战斗/CODEX），把 `.py` 丢进 `plugins/` 即用，无商店、无审核。

---

## 十四、界面

- 分色气泡 + 默认头像：每个角色自动分配颜色与首字圆形头像（无需图片文件）
- 🎨 现代界面：强调色/深色·浅色·OLED/圆角/紧凑模式（➕ 面板「🎨 主题」）
- ➕ 微信式面板：写 Word/Excel、读文档、导出、插件动作一站式收纳
- 侧栏折叠：顶部「◀ 收起侧栏」沉浸式聊天
- 创意工坊：内置工坊客户端（浏览/搜索/下载/上传/点赞/删除），默认连接本机 http://localhost:5000，可在工坊窗口修改服务器地址。开服务器属于分享者进阶操作：用源码包附带的 net.py 启动（需 Python + Flask）；逛工坊、下载角色只需客户端即可
- 中英双语界面：英文 Windows 首次启动自动切换英文 UI；字体设置行右侧「中/EN」按钮可随时手动切换（重启后全面生效）

---

## 十五、命令大全

| 命令 | 功能 |
|---|---|
| /swipe [back 或 N] | 多候选回复生成与切换 |
| /穿越 [世界名] | 平行世界穿越 |
| /memory [recall 或 summary 或 clear] | 记忆链 |
| /read [路径]、/readinfo、/readclear | 文档读入 |
| /doc、/office | 生成 Word/Excel |
| /财报 [爬取/标题/入库/检索/定时/股票] | 政策深爬、政策库与个股分析 |
| /股票 <代码> | A股炒股要素分析（技术指标全套） |
| /联动 <代码,代码> [关键词] | 联动综合分析：多股技术面 + 政策库 + 结论 |
| /股票列表 [N]（跌 [N]） | 沪深北全市场涨跌幅榜 |
| /全市场 涨幅>5 换手>10 | 全市场条件筛选（5900+ 只） |
| /爬取 <网址> | 通用网页爬取 |
| /搜索 <词>、/深搜 <词> | 联网搜索（深搜自动跟进抓正文） |
| /jp、/zh | 日中互译 |
| /tts [speakers 或 voice 或 on 或 off 或 stop] | 语音朗读 |
| /r 2d6、/d20 | 掷骰 |
| /dice | 骰子帮助 |

---

## 十六、常见问题（便携版）

**Q：双击没反应或闪退？**
查看 exe 旁的 debug.log 最后几行；确认整个文件夹解压完整（DICK.exe 与 _internal 同级）；将文件夹加入杀软白名单。

**Q：提示缺少 DLL 或文件？**
解压不完整，或被杀软删了文件 → 重新解压并加白名单。

**Q：我的存档存在哪里？**
exe 旁的 saves 文件夹；设置与 API Key 在 exe 旁 config.json。全部数据不写注册表。

**Q：更新程序后聊天记录没了？**
数据都在旧文件夹里。把旧文件夹的 saves/memory/worlds/personas/prompt_presets/plugin_settings/exports 拷到新版文件夹即可。

**Q：预设菜单只剩「默认」一项？**
prompt_presets 文件夹里的预设被误删。把整个 prompt_presets 文件夹删掉，重启程序会自动重新释放内置预设。

**Q：只有 DICK.exe 一个文件能运行吗？**
不能。_internal 文件夹必须和 DICK.exe 在一起，移动时请整文件夹移动。

**Q：TTS 没有声音？**
确认 VOICEVOX 已启动；未安装 VOICEVOX 时走 edge-tts 备胎（需联网）。

**Q：财报爬取失败？**
政务网站改版或网络受限常见。用 /财报 爬取 <网址> 手动补充；联网搜索需本机可访问 DuckDuckGo/Bing。

**Q：群聊里角色名字显示「角色1」？**
该角色卡缺少 name 字段。打开卡 JSON，在顶层加 "name": "角色名" 即可。

**Q：聊久了 AI 失忆？**
开启「📜 滚动摘要」+ 选好「🧮 预算」+ 记忆链 /memory recall，三保险。

**Q：想要英文界面？**
英文系统自动切换英文；或点击字体设置行右侧「中/EN」按钮切换，重启程序后生效。插件命令（/股票 等）暂保持中文。

---

## 十七、免责声明

- 财报模式、联网搜索产出的所有分析仅供参考，不构成任何投资建议
- 政策爬取仅访问公开官方页面，请遵守目标网站的使用条款
- 本项目仅供学习与个人使用
